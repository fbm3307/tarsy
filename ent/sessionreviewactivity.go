// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/codeready-toolchain/tarsy/ent/alertsession"
	"github.com/codeready-toolchain/tarsy/ent/sessionreviewactivity"
)

// SessionReviewActivity is the model entity for the SessionReviewActivity schema.
type SessionReviewActivity struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// SessionID holds the value of the "session_id" field.
	SessionID string `json:"session_id,omitempty"`
	// User who performed the action (X-Forwarded-User)
	Actor string `json:"actor,omitempty"`
	// What happened
	Action sessionreviewactivity.Action `json:"action,omitempty"`
	// Review status before transition
	FromStatus *sessionreviewactivity.FromStatus `json:"from_status,omitempty"`
	// Review status after transition
	ToStatus sessionreviewactivity.ToStatus `json:"to_status,omitempty"`
	// Set when action is resolve
	ResolutionReason *sessionreviewactivity.ResolutionReason `json:"resolution_reason,omitempty"`
	// Free-text context
	Note *string `json:"note,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SessionReviewActivityQuery when eager-loading is set.
	Edges        SessionReviewActivityEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SessionReviewActivityEdges holds the relations/edges for other nodes in the graph.
type SessionReviewActivityEdges struct {
	// Session holds the value of the session edge.
	Session *AlertSession `json:"session,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// SessionOrErr returns the Session value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SessionReviewActivityEdges) SessionOrErr() (*AlertSession, error) {
	if e.Session != nil {
		return e.Session, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: alertsession.Label}
	}
	return nil, &NotLoadedError{edge: "session"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SessionReviewActivity) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sessionreviewactivity.FieldID, sessionreviewactivity.FieldSessionID, sessionreviewactivity.FieldActor, sessionreviewactivity.FieldAction, sessionreviewactivity.FieldFromStatus, sessionreviewactivity.FieldToStatus, sessionreviewactivity.FieldResolutionReason, sessionreviewactivity.FieldNote:
			values[i] = new(sql.NullString)
		case sessionreviewactivity.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SessionReviewActivity fields.
func (_m *SessionReviewActivity) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sessionreviewactivity.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case sessionreviewactivity.FieldSessionID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field session_id", values[i])
			} else if value.Valid {
				_m.SessionID = value.String
			}
		case sessionreviewactivity.FieldActor:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field actor", values[i])
			} else if value.Valid {
				_m.Actor = value.String
			}
		case sessionreviewactivity.FieldAction:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field action", values[i])
			} else if value.Valid {
				_m.Action = sessionreviewactivity.Action(value.String)
			}
		case sessionreviewactivity.FieldFromStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field from_status", values[i])
			} else if value.Valid {
				_m.FromStatus = new(sessionreviewactivity.FromStatus)
				*_m.FromStatus = sessionreviewactivity.FromStatus(value.String)
			}
		case sessionreviewactivity.FieldToStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field to_status", values[i])
			} else if value.Valid {
				_m.ToStatus = sessionreviewactivity.ToStatus(value.String)
			}
		case sessionreviewactivity.FieldResolutionReason:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field resolution_reason", values[i])
			} else if value.Valid {
				_m.ResolutionReason = new(sessionreviewactivity.ResolutionReason)
				*_m.ResolutionReason = sessionreviewactivity.ResolutionReason(value.String)
			}
		case sessionreviewactivity.FieldNote:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field note", values[i])
			} else if value.Valid {
				_m.Note = new(string)
				*_m.Note = value.String
			}
		case sessionreviewactivity.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SessionReviewActivity.
// This includes values selected through modifiers, order, etc.
func (_m *SessionReviewActivity) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySession queries the "session" edge of the SessionReviewActivity entity.
func (_m *SessionReviewActivity) QuerySession() *AlertSessionQuery {
	return NewSessionReviewActivityClient(_m.config).QuerySession(_m)
}

// Update returns a builder for updating this SessionReviewActivity.
// Note that you need to call SessionReviewActivity.Unwrap() before calling this method if this SessionReviewActivity
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SessionReviewActivity) Update() *SessionReviewActivityUpdateOne {
	return NewSessionReviewActivityClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SessionReviewActivity entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SessionReviewActivity) Unwrap() *SessionReviewActivity {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SessionReviewActivity is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SessionReviewActivity) String() string {
	var builder strings.Builder
	builder.WriteString("SessionReviewActivity(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("session_id=")
	builder.WriteString(_m.SessionID)
	builder.WriteString(", ")
	builder.WriteString("actor=")
	builder.WriteString(_m.Actor)
	builder.WriteString(", ")
	builder.WriteString("action=")
	builder.WriteString(fmt.Sprintf("%v", _m.Action))
	builder.WriteString(", ")
	if v := _m.FromStatus; v != nil {
		builder.WriteString("from_status=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("to_status=")
	builder.WriteString(fmt.Sprintf("%v", _m.ToStatus))
	builder.WriteString(", ")
	if v := _m.ResolutionReason; v != nil {
		builder.WriteString("resolution_reason=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := _m.Note; v != nil {
		builder.WriteString("note=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SessionReviewActivities is a parsable slice of SessionReviewActivity.
type SessionReviewActivities []*SessionReviewActivity
